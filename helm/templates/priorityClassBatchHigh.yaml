apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: {{ .Values.priorityClassBatchHigh.name | default "batch-high-prio" }}
value: {{ .Values.priorityClassBatchHigh.value | default -50 }}
globalDefault: false
description: "High priority for batch workloads"