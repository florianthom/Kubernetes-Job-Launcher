apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: {{ .Values.priorityClassBatchLow.name | default "batch-low-prio" }}
value: {{ .Values.priorityClassBatchLow.value | default -100 }}
globalDefault: false
description: "Low priority for batch workloads"